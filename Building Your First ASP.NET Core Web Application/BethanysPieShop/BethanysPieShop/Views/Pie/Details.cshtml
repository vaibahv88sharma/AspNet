@model Pie

@*<h2>@Model.Name</h2>

<p><span>@Model.ShortDescription</span></p>
<p><span>@Model.LongDescription</span></p>
<p><span>@Model.AllergyInformation</span></p>
<p><span>@Model.Price</span></p>
<p>
    <img alt="@Model.Name" src="@Url.Content(@Model.ImageUrl)" />
</p>

<p class="button">
    <a asp-controller="ShoppingCart" asp-action="AddToShoppingCart"
       asp-route-pieId="@Model.PieId">Add to cart</a>
</p>*@



<div class="thumbnail">
    <img alt="@Model.Name" src="@Model.ImageUrl">
    <div class="caption-full">
        <h3 class="pull-right">@Model.Price</h3>
        <h3>
            <a href="#">@Model.Name</a>
        </h3>
        <h4>@Model.ShortDescription</h4>
        <p>@Model.LongDescription</p>
        <p class="button">
            <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="AddToShoppingCart"
               asp-route-pieId="@Model.PieId">Add to cart</a>
        </p>
    </div>
</div>
